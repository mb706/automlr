#!/bin/sh

if [ -z "$R_HOME" ] ; then
  RBIN="/usr/bin/env Rscript"
else
  RBIN="${R_HOME}/bin/Rscript"
fi

#sourcepath=$(dirname "$(readlink -f "$0")")
#cd "$sourcepath"

cd tools
./searchspaceconvert.py
cd ..

${RBIN} -e "roxygen2::roxygenise()"



