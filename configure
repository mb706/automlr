#!/bin/sh

sourcepath=$(dirname "$(readlink -f "$0")")
cd "$sourcepath"

cd tools
./searchspaceconvert.py
cd ..

${R_HOME}/bin/R --vanilla <<EOF
library(checkmate)
library(roxygen2)
roxygenise()
EOF


